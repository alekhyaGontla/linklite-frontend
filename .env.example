# Frontend (Vite) — these must live at project root, not in src/server,
# because Vite only auto-loads .env files from envDir (defaults to root).
VITE_API_BASE_URL="https://your-linklite-backend.onrender.com";
VITE_AI_API_BASE_URL=http://localhost:3000/api
